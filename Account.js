import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Account = (props) => {
    return (
        <View>

        </View>
    );
}

const styles = StyleSheet.create({

});

export { Account };

